package texteditor;
import static org.assertj.core.api.Assertions.*;
import org.junit.jupiter.api.Test;
public class TextEditorTests {
    public void testTextEditor() {
        TextEditor textEditor = new TextEditor();
        textEditor.execute(1);

    }
}
